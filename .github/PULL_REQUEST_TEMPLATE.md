<!-- What does this change and why? -->

## Summary

## Checklist

- [ ] `go build ./...`, `go vet ./...`, `go test ./...` pass
- [ ] `npm run build` passes in `dashboard/`
- [ ] Commits are signed off (`git commit -s`) and I accept the [CLA](../CLA.md)
- [ ] For a new detection rule: added an example it catches and one it should not flag
